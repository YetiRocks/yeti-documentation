name: "Documentation"
app_id: "documentation"
customer_id: "yeti"
version: "1.0.0"
description: "Documentation"

static:
  path: web
  root: /
  build:
    source: source
    command: for book in learn guides reference; do (cd books/$book && mdbook build); done && mkdir -p ../web/assets && cp assets/*.svg assets/*.png ../web/assets/ && cp books/index.html ../web/index.html
